on: 
  push:
  workflow_dispatch:

env:
  greeting: "Hello"
  name: "Dan"

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v2 
      - uses: actions/setup-python@v4
        with:
         python-version: '3.9' 
      - run: pwd
      - run: ls
      # - run: python ${GITHUB_WORKSPACE}/myscrypt.py

      - run: bash helloworld.sh